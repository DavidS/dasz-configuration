source 'https://rubygems.org'

puppetversion = ENV['PUPPET_VERSION']
gem 'puppet', puppetversion, :require => false
gem 'puppet-lint'
gem 'puppetlabs_spec_helper', '>= 0.1.0'
group :development do
  gem 'guard-rspec'
  # required for interactive guard
  gem 'rb-readline'
end
