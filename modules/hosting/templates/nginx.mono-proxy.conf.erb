location <%= @location %> {
	index index.html index.htm default.aspx Default.aspx;
	fastcgi_index Default.aspx;
	fastcgi_pass unix:<%= @base_dir %>/run/<%= @local_name %>.sock;
	include /etc/nginx/fastcgi_params;
}
